#spring:
#  application:
#    name: eureka-server
#  lifecycle:
#    timeout-per-shutdown-phase: 10s
#  security:
#    user:
#      name: be12-team1
#      password: nhnacademy123! # <--- 서버 A가 접속할 때 사용
#
#server:
#  shutdown: graceful
#  port: 10420 # <--- B의 포트
#
#eureka:
#  client:
#    # 이중화를 위해 서로에게 등록해야 합니다. (추가/수정)
#    register-with-eureka: true
#    # 이중화를 위해 서로의 레지스트리를 가져와야 합니다. (추가/수정)
#    fetch-registry: true
#    service-url:
#      # 🎯 상대방(Peer A: 10419)의 주소를 defaultZone에 지정합니다.
#      defaultZone: http://${spring.security.user.name}:${spring.security.user.password}@127.0.0.1:10419/eureka/
#
#  instance:
#    prefer-ip-address: true
#    instance-id: ${spring.application.name}:${server.port}
#    registry:
#      # 피어 복제 시 0으로 설정하는 것이 일반적이며 올바른 설정입니다.
#      default-open-for-traffic-count: 0